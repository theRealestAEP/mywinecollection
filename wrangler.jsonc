// Cloudflare serves the built book (dist/display) as static files.
// `npx wrangler deploy` uploads them to a Worker with this name.
{
  "name": "mywinecollection",
  "compatibility_date": "2026-09-21",
  "assets": { "directory": "./dist/display" }
}
